import React from "react";
import ReactDOM from "react-dom/client";
import Password_generator from "./App.jsx";
let body = ReactDOM.createRoot(document.querySelector("#body"));
body.render(<Password_generator />);
